#include "borrowing.h"

void borrowing:: display(){
        if(id_book != 9999){
        std::cout << std::setw(x)  << "ID person : " << std::setw(5) << id_person << std::setw(x) << "ID book : " << std::setw(5) << id_book << std::endl;
        }
    }
void borrowing:: check(){

    }
